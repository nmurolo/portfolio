---
import background from '../assets/background.svg';
---

<div id="container">
	<img id="background" src={background.src} alt="" fetchpriority="high" />
	<main>
		<section id="hero">
			<h1>
				Thanks for visiting my website. <br /> Check back soon :)
			</h1>
			
			<div id="links">
				<!-- 
					resume.pdf lives in the /public folder
				-->
				<a href="/resume.pdf" class="button" download="Nicholas_Murolo_Resume.pdf">
					Download my resume
					<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
						<path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 0 0 5.25 21h13.5A2.25 2.25 0 0 0 21 18.75V16.5M16.5 12 12 16.5m0 0L7.5 12m4.5 4.5V3" />
					</svg>
				</a>
			</div>

		</section>
	</main>
</div>

<style>
	*, *::before, *::after {
		box-sizing: border-box;
	}

	#background {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: -1;
		filter: blur(100px);
		object-fit: cover; 
	}

	#container {
		font-family: Inter, Roboto, 'Helvetica Neue', 'Arial Nova', 'Nimbus Sans', Arial, sans-serif;
		min-height: 100vh; 
		display: flex;
		flex-direction: column;
	}

	main {
		flex: 1;
		display: flex;
		justify-content: center;
		align-items: center; 
		width: 100%;
	}

	#hero {
		display: flex;
		align-items: flex-start;
		flex-direction: column;
		justify-content: center;
		padding: 2rem;
		max-width: 800px;
		width: 100%;
	}

	h1 {
		font-family: ui-monospace, 'Cascadia Code', 'Source Code Pro', Menlo, Consolas, 'DejaVu Sans Mono', monospace;
		font-weight: 700;
		font-size: 28px;
		line-height: 1.4;
		margin-top: 0;
		margin-bottom: 2rem;
		
		background: linear-gradient(14deg, #1d2ff0 0%, #8033f3 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		
		white-space: normal; 
		text-wrap: balance; 
	}

	.mobile-break {
		display: none;
	}

	#links {
		display: flex;
		gap: 16px;
	}

	#links a {
		display: flex;
		align-items: center;
		padding: 12px 20px;
		color: #111827;
		text-decoration: none;
		transition: all 0.2s ease;
		font-weight: 500;
	}

	#links a:hover {
		color: rgb(78, 80, 86);
	}

	#links a svg {
		height: 1.2em;
		width: 1.2em;
		margin-left: 8px;
	}

	#links a.button {
		color: white;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		box-shadow:
			inset 0 0 0 1px rgba(255, 255, 255, 0.12),
			inset 0 -2px 0 rgba(0, 0, 0, 0.24);
		border-radius: 10px;
	}

	#links a.button:hover {
		color: rgb(255, 255, 255);
		box-shadow: none;
		transform: translateY(-1px);
	}

	h2 {
		margin: 0 0 1em;
		font-weight: normal;
		color: #111827;
		font-size: 20px;
	}

	p {
		color: #4b5563;
		font-size: 16px;
		line-height: 24px;
		letter-spacing: -0.006em;
		margin: 0;
	}

	code {
		display: inline-block;
		background:
			linear-gradient(66.77deg, #5d17ce 0%, #2a11e6 100%) padding-box,
			linear-gradient(155deg, #16c2e0 0%, #3bff59 18%, #aff1ec 45%) border-box;
		border-radius: 8px;
		border: 1px solid transparent;
		padding: 6px 8px;
	}

	.box {
		padding: 16px;
		background: rgba(255, 255, 255, 1);
		border-radius: 16px;
		border: 1px solid white;
	}

	#news {
		position: absolute;
		bottom: 16px;
		right: 16px;
		max-width: 300px;
		text-decoration: none;
		transition: background 0.2s;
		backdrop-filter: blur(50px);
	}

	#news:hover {
		background: rgba(255, 255, 255, 0.55);
	}

	@media screen and (max-height: 368px) {
		#news {
			display: none;
		}
	}

	/* Mobile Specific Styles */
	@media screen and (max-width: 768px) {
		#hero {
			align-items: center; /* Center content horizontally */
			text-align: center;
			padding: 16px;
		}

		h1 {
			font-size: 24px; /* Slightly smaller on mobile */
			line-height: 1.5;
		}
		
		/* Force a line break on mobile for better aesthetics */
		.mobile-break {
			display: block;
		}

		#links {
			width: 100%;
			justify-content: center;
		}

		#links a.button {
			width: 100%;
			justify-content: center;
			padding: 16px 20px; /* Larger touch target */
		}

		#news {
			right: 16px;
			left: 16px;
			bottom: 2.5rem;
			max-width: 100%;
		}
	}
</style>

